parameters:
  script: サンプルスクリプト

commands:
  - type: sample
    comment: サンプルデータを返却
    debug: no
    parameters: {}

  - type: echo
    comment: エコーサンプル１
    debug: no
    parameters:
      target_keys:
        - p1
        - p2
        - p4

parameters:
  script: サンプルスクリプト

commands:
  - type: sample
    comment: サンプルデータを返却
    parameters:
      targets:
        - p1
        - p2
        - p4

  - type: echo
    comment: エコーサンプル１
    parameters:
      targets:
        - p1
        - p2
        - p4

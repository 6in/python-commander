comment: サンプルコマンドからデータを取得して、内容をコンソールに出力する

parameters:
  param1: sample

commands:
  - type: sample
    comment: サンプルデータを返却
    debug: no
    parameters: {}

  - type: echo
    comment: エコーサンプル１
    debug: no
    parameters:
      target_keys:
        - p1
        - p2
        - p4

name: Echo
comment: 指定した列名を画面に表示する
parameters:
  target_keys:
    type: list
    comment: 列名を配列で指定する
    require: true
    default: []

sample: |
  type: Echo
  comment: 行単位で、指定したカラムの内容を表示
  target_keys:
    - key1
    - key2
    - key3
